[project]
name = "jungsanjungsan-be"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "siwon", email = "ksw3721@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.19.1",
    "fastapi[standard]>=0.141.1",
    "psycopg[binary]>=3.3.4",
    "pydantic-settings>=2.15.0",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
jungsanjungsan-be = "jungsanjungsan_be:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
